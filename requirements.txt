fastapi==0.115.0
uvicorn[standard]==0.30.6
# C/Rust-extension packages below use >= floors instead of exact pins —
# the exact versions originally tested predate Python 3.13 wheel builds for
# several of them (psycopg-binary, bcrypt, cryptography all hit this on the
# deploy VM's very-new Debian 13 / Python 3.13), forcing slow, memory-heavy
# source compiles on constrained hardware. A floor keeps behavior close to
# what was tested while letting pip pick a version with an actual wheel.
sqlalchemy>=2.0.35,<2.1
alembic==1.13.2
psycopg[binary]>=3.2.1,<4.0
pydantic>=2.9.2,<3.0
pydantic-settings==2.5.2
bcrypt>=4.0.1
itsdangerous==2.2.0
python-multipart==0.0.9
jinja2==3.1.4
cryptography>=43.0.1,<44.0
apscheduler==3.10.4
dhanhq
pandas>=2.2.2,<3.0

# Dev / tests
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
